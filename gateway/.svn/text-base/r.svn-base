#!/bin/bash

name=lcs_gateway

pwd=$(readlink -f $(dirname $0))
ln -sf /home/s/bin/lddskel $name
./$name \
    --cfg-dir=$pwd/../cfg \
    --pid-dir=$pwd/ \
    --lib-dir=$pwd/ \
    --log-dir=$pwd/log \
    --log-level=0 \
    --auto-restart=0 \
    $@
rm -f $name
